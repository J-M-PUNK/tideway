[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Quiet the default collection summary for small suites; individual
# failures still print in full.
addopts = -q --tb=short
